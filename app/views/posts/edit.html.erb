<%=form_for @post do |post|%><%#the barebone post edit %>
    Title: <%=post.text_field :title %>
    <br>
    <br>
      Body: <%=post.text_area :body, cols: 30, rows: 5%>
    <br>
    <br>
    Tags:
      <%= post.collection_check_boxes( :tag_ids, Tag.all, :id, :name, {}, :multiple => true)%>
    <br>
    <br>
    Category test #1<%#category id test # 1%>
      <%#=post.collection_radio_buttons( :category_id,
                                        Category.all,
                                        :id,
                                        :name
                                      )%>
    <br>
    <br>
    Category test #2<%#category id test # 2%>
      <%=post.collection_select(  :category_id,
                                  Category.all, 
                                  :id, 
                                  :name,
                                  {:include_blank => true}
                                )%>
    <br>
    <br>
    <%=post.submit%>
<%end%>
<%#=form_tag(:controller => "post", :action => "update", :class => :post) do%>
    Category test #3<%#category id test # 2%>
      <%#= select_tag( :category_id,
                     options_for_select(@category_selection, @post.category_id)
                     ) %>
    <%#=submit_tag "submit"%>
<%#end%>


