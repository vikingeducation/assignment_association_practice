<%= @post.inspect %>
<hr>
<% @post.tags.each do |t| %>
  <%= "#{t.name}," %>
<% end %>
<hr>
<% @post.users.each do |u| %>
  <%= "#{u.name}," %>
<% end %>
<hr>
<% @post.comments.each do |c| %>
 <%= c.inspect %>
<% end %>

<hr>

<%= link_to 'Edit', edit_post_path(@post.id) %>