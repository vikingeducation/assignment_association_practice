<%= form_for post do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>
	<br>
	<%= f.label :body %>
	<%= f.text_area :body %>
	<br>
	<%= f.label :tag_ids %>
	<%= f.collection_check_boxes(:tag_ids, Tag.all, :id, :name) %>
	<%= f.submit %>
<% end %>
