<H2>Create new post</H2>

<%= form_for @post do |f| %>
  <%= f.text_field :title %><br>
  <%= f.text_area :body, size: "40x40"  %><br>
  <p>Chose tags for the post</p>
  <%= f.collection_check_boxes(:tag_ids,
                               Tag.all,
                               :id,
                               :name)  %><br>
  <%= f.submit "Create Post" %>
<% end %>
