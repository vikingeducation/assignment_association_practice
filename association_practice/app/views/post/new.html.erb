<%= form_for @post do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br>
  <%= f.label :body %>
  <%= f.text_field :body %>
  <br>
  <%= f.label :tags %>
  <br>
  <%= f.collection_check_boxes(
    :tag_ids,
    Tag.all,
    :id,
    :name
    ) %>


  <%= f.submit %>

<% end %> 